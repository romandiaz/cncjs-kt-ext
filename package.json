{
  "name": "cncjs-kt-ext",
  "version": "1.0.0",
  "description": "",
  "keywords": [
    "cncjs",
    "widget",
    "extension"
  ],
  "main": "src/extension/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/extension/index.js",
    "build": "node -e \"const fs = require('fs'); if (fs.existsSync('dist')) { fs.rmSync('dist', {recursive: true, force: true}); } fs.mkdirSync('dist'); const files = fs.readdirSync('src/widget'); files.forEach(file => fs.copyFileSync('src/widget/' + file, 'dist/' + file));\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.20.3",
    "gcode-toolpath": "^2.2.0",
    "jsonwebtoken": "^9.0.0",
    "lodash.get": "^4.4.2",
    "socket.io-client": "^2.3.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-standard": "^12.0.0",
    "eslint-plugin-import": "^2.18.2",
    "eslint-plugin-node": "^8.0.1",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.1"
  }
}